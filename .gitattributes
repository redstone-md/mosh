# Shell scripts must keep LF endings. release.yml runs scripts/release-notes.sh
# on the Windows runner too, where the default core.autocrlf=true would otherwise
# check it out with CRLF and bash would fail on every line with $'\r'.
*.sh text eol=lf
